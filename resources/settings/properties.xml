<properties>
    <!-- Course definition: semicolon-separated crossings "code:cumulative_m".
         code = S (start), L (lap), F (finish). Example: "S:0;L:30;L:60;F:100" -->
    <property id="course1" type="string">S:0;F:30</property>
    <property id="course2" type="string">S:0;L:30;L:60;F:100</property>
    <property id="course3" type="string">S:0;L:100;L:200;L:300;F:400</property>
    <!-- 4-8 are empty until the athlete fills them in; an empty slot is
         skipped by the on-watch course menu rather than shown as a course. -->
    <property id="course4" type="string"></property>
    <property id="course5" type="string"></property>
    <property id="course6" type="string"></property>
    <property id="course7" type="string"></property>
    <property id="course8" type="string"></property>
    <property id="activeCourse" type="number">1</property>

    <!-- Quick course distance in metres, set on the watch (issue #22). 0 means
         no quick course is in force and activeCourse decides. -->
    <property id="quickDistanceM" type="number">0</property>

    <!-- Estimated BLE delivery latency, ms (see docs/REVERSE-ENGINEERING.md). -->
    <property id="bleLatencyMs" type="number">150</property>

    <!-- Clear record-level developer fields on the record after a split is written. -->
    <property id="clearAfterWrite" type="boolean">true</property>

    <!-- Log raw packets for reverse-engineering; shows hex on screen. -->
    <property id="captureMode" type="boolean">false</property>

    <!-- Also call addLap() on every crossing (not just FINISH). -->
    <property id="lapPerCrossing" type="boolean">false</property>

    <!-- The FxChip's printed id, "2 letters - 4 digits" (issue #64). Typed by
         the athlete in Garmin Connect and stored on the watch; empty means no
         chip is bound and the app records nothing rather than guessing. -->
    <property id="chipId" type="string"></property>

    <!-- Superseded by chipId. The chip's advertised *name* was the only
         identity we thought we had; the real printed id is in every frame
         (#63). Removed with the connection-oriented BLE layer in #61, which is
         what still writes it - there is nothing to replace it with until the
         decoder lands. -->
    <property id="lastChipName" type="string"></property>
</properties>
